<body style="margin:0;">
<svg id="autoFitSVG" xmlns="http://www.w3.org/2000/svg">
  <foreignObject id="foreign" xmlns="http://www.w3.org/2000/svg">
    <div xmlns="http://www.w3.org/1999/xhtml">
      <style>
        .text {
          display: inline-block;
          animation: typing 1.5s steps(9), blink .5s step-end infinite alternate;
          white-space: nowrap;
          overflow: hidden;
          border-right: 3px solid;
          font-family: monospace;
          font-size: 2em;
          background: linear-gradient(90deg, rgba(0,241,220,1) 0%, rgba(86,115,241,1) 44%, rgba(103,68,241,1) 54%, rgba(254,0,255,1) 100%);
          -webkit-background-clip: text;
          -webkit-text-fill-color: transparent;
        }

        @keyframes typing {
          from { width: 0 }
        }

        @keyframes blink {
          from, to { border-color: transparent }
          50% { border-color: rgba(254,0,255,1); }
        }
      </style>
      <div class="text" id="textContent">Hi 👋, I'm Mostafa Mahmoud</div>
    </div>
  </foreignObject>
</svg>

<script>
  window.addEventListener('load', () => {
    const svg = document.getElementById('autoFitSVG');
    const foreign = document.getElementById('foreign');

    // Use a timeout to allow animation DOM to settle
    setTimeout(() => {
      const bbox = svg.getBBox();
      svg.setAttribute('width', bbox.width);
      svg.setAttribute('height', bbox.height);
      foreign.setAttribute('width', bbox.width);
      foreign.setAttribute('height', bbox.height);
    }, 100); // small delay to let text render
  });
</script>
</body>
